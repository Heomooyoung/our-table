# 우리집 식탁 — 작업 규칙

## 커밋할 때 개발일지를 같이 쓴다 (필수)

커밋 하나 = 일지 한 문단. **나중에 몰아 쓰지 않는다.**

- 파일: `/mnt/c/Users/정은/OneDrive/Desktop/옵시디언/업무/20260806 우리집 식탁 앱 개발일지.md`
- 형식: `## 추가 (MM-DD, vX.Y) — 한 줄 제목` 뒤에 **뭘 바꿨나 / 왜 그렇게 했나 / 교훈**
- 버그면 **증상 → 원인 → 왜 늦게 찾았는지**까지 남긴다
- 사용자 지적으로 바뀐 것은 **그 말을 그대로 인용**한다
- 일이 끝나면 일지의 `## 남은 일`을 현재 상태로 맞춘다
- 책 소재가 될 교훈은 `## 오늘의 교훈` 아래 날짜별로 모은다

자세한 이유는 `README.md`의 "개발일지" 절에 있다.

## 그 밖에

- 앱은 빌드 없는 단일 파일 `index.html`. 프레임워크·번들러 없음
- 서버 변경은 `supabase/schema*.sql`에 남기고, 적용 순서는
  `schema.sql` → `analytics.sql` → `schema-v2.sql` → `schema-v3.sql` → `schema-v4.sql`
- 배포는 `main`에 push하면 Vercel이 자동으로 한다
- 화면을 바꿨으면 `npm test`(jsdom UX 테스트)를 돌리고 커밋한다
- 서비스워커 캐시 이름(`sw.js`의 `V`)은 화면이 바뀔 때마다 올린다.
  안 올리면 폰에 옛 화면이 남는다
